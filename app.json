{
  "name": "Boss Bot MINI",
  "description": "WhatsApp MD bot (Baileys)",
  "repository": "",
  "keywords": ["whatsapp", "baileys", "bot", "node"],
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-nodejs.git"
    }
  ],
  "env": {
    "SESSION_ID": {
      "description": "BossBot session string (e.g. BossBot!<base64_compressed_data>)",
      "required": false,
      "value": ""
    },
    "MONGODB_URI": {
      "description": "MongoDB connection string for session storage",
      "required": true
    }
  }
}

